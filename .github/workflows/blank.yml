name: Hello Workflow

on:
  push:
   branches:
    - main
    - 'release/**'
  pull_request:
   branches:
    - main  

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello step
        run: |
         echo "this is line 1"
         echo "this is line"
